Original code for modified files is in original/.

To build the multi-threaded version with the "cheap" optimizations on, compile as follows:

    make version=pthreads cheap=1
    
Test with the following arguments ("native.runconf" in Parsec):

    ./swaptions -ns 128 -sm 1000000 -nt ${NTHREADS}

e.g., `-nt 1`
